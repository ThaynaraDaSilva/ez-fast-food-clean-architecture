apiVersion: apps/v1
kind: Deployment
metadata:
  name: ez-fast-food-deployment
  namespace: ez-fast-food
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ez-fast-food
  template:
    metadata:
      labels:
        app: ez-fast-food
    spec:
      containers:
      - name: ez-fast-food-container
        image: 	  dasilvathaynara/ez-fast-food-api:lastest
        ports:
        - containerPort: 8080
        envFrom:
        - configMapRef:
            name: ez-fast-food-configmap-app   # Reference the created ConfigMap
        resources:
          requests:
            cpu: "300m"
            memory: "200Mi"
          limits:
            cpu: "660m"
            memory: "440Mi"